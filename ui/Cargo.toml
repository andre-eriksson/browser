[package]
name = "ui"
version = "0.1.0"
edition = "2024"

[dependencies]
iced = { version = "0.13.1", features = [
    "multi-window",
    "tokio",
    "image",
    "svg",

] }
image = "0.25"
assets = { path = "../assets" }
tracing = { workspace = true }
api = { path = "../api" }
html-parser = { path = "../html-parser" }
network = { path = "../network" }
