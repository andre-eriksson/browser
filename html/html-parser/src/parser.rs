//! HTML Parser Module
//!
//! Contains implementations for parsing HTML documents, including both complete document parsing and streaming parsing.

/// A streaming based parser which incrementally parses HTML documents in chunks.
pub mod streaming;
