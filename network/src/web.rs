/// The builder module provides a way to construct and configure HTTP clients.
pub mod builder;

/// The client module handles all client-related functionality like making HTTP requests and managing sessions.
pub mod client;
